{
  "name": "run-agent",
  "version": "0.1.0",
  "description": "Local runtime for coding agents — session management, process supervision, event normalization, git isolation",
  "type": "module",
  "bin": {
    "ra": "./dist/cli/index.js",
    "run-agent": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/cli/index.js",
    "test": "vitest run",
    "doctor": "node dist/cli/index.js doctor"
  },
  "keywords": ["agent", "coding-agent", "runtime", "claude", "codex", "opencode", "omp"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
